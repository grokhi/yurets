# Юрец ФМ — переменные окружения
# Скопируйте в .env и заполните нужное.

# ---- Основное ----
YURETS_STREAM_MIME_TYPE=audio/mpeg
YURETS_CHUNK_SIZE=65536

# ---- Локальная библиотека ----
# В docker-compose папка ./music монтируется в контейнер как /music
YURETS_LOCAL_MUSIC_DIR=/music

# ---- Расписание (JSON) ----
# Источники: "telegram" и "local" (сейчас).
# Время в формате HH:MM, конец слота не включается.
# Слот может "пересекать" полночь (например 18:00–00:00).
YURETS_SCHEDULE_JSON=[{"start":"00:00","end":"08:00","source":"telegram"},{"start":"08:00","end":"18:00","source":"local"},{"start":"18:00","end":"00:00","source":"telegram"}]

# ---- Telegram источник ----
# Для работы через Docker рекомендован bot token.
# Бот должен иметь доступ к каналу (быть участником; для приватного — приглашён).
YURETS_TELEGRAM_API_ID=
YURETS_TELEGRAM_API_HASH=
YURETS_TELEGRAM_BOT_TOKEN=

# Можно @channelname или https://t.me/... или числовой id
YURETS_TELEGRAM_CHANNEL=

# Где хранить сессию Telethon (в docker-compose монтируется ./telegram_session)
YURETS_TELEGRAM_SESSION=/telegram_session/yurets_fm.session

# Сколько последних сообщений просматривать при поиске треков
YURETS_TELEGRAM_FETCH_LIMIT=50
